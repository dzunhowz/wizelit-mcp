# AWS Configuration
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_REGION=us-east-1
CHAT_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0

# AWS Bedrock Defaults (optional)
DEFAULT_AWS_REGION=us-east-1
DEFAULT_BEDROCK_MODEL=anthropic.claude-3-haiku-20240307-v1:0

# Database Connection Pool Configuration (optional - if using database)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_ECHO_SQL=false

# GitHub (optional)
GITHUB_TOKEN=your_github_token

# GitHub Cache Configuration (optional)
GITHUB_CACHE_DIR=/tmp/github_cache
GITHUB_CACHE_MAX_AGE_HOURS=24
GITHUB_CACHE_MAX_SIZE_MB=5000

# Code Scout Configuration (optional)
CODE_SCOUT_USE_CACHE=true

# Redis (optional - for log streaming)
REDIS_URL=redis://localhost:6379
ENABLE_LOG_STREAMING=true

# Job Configuration
JOB_LOG_TAIL=25

# CrewAI Configuration (uses CHAT_MODEL_ID by default)
CREWAI_TIMEOUT_SECONDS=120
